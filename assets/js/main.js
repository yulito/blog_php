//ejecutar link del select categorias
function cat(value) {
    if (value != "") { location.href = value; }
}

//ejecutar link del select perfil
function perfil(value) {
    if (value != "") { location.href = value; }
}